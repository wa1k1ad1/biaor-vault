```dataviewjs
// 获取所有笔记文件（排除当前文件）
const pages = dv.pages()
    .where(p => p.file.name !== dv.current().file.name);

const allNotes = [];

for (let page of pages) {
    const content = await dv.io.load(page.file.path);
    const lines = content.split('\n');
    
    for (let i = 0; i < lines.length; i++) {
        const line = lines[i].trim();
        const match = line.match(/^机会[:：]{2}\s*(.+)$/);
        if (match) {
            const contentText = match[1].trim();
            
            allNotes.push({
                content: contentText,
                sourceFile: page.file.path,
                lineNumber: i,
                sourceNote: page.file.name,
                date: new Date(page.file.ctime).toLocaleDateString('zh-CN'),
                ctime: page.file.ctime
            });
        }
    }
}

// 按内容倒序排序（最新内容在最前）
const sortedNotes = allNotes.sort((a, b) => 
    b.content.localeCompare(a.content)
);

// 创建容器
const container = dv.el('div', '');
container.style.maxWidth = '900px';

// 添加样式（包含文字环绕效果）
const style = document.createElement('style');
style.textContent = `
.note-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 20px;
    margin-top: 20px;
}
.note-card {
    border: 1px solid #eaeaea;
    border-radius: 10px;
    padding: 20px;
    background: white;
    transition: all 0.3s;
    height: 100%;
    display: flex;
    flex-direction: column;
}
.note-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    border-color: #4da6ff;
}
.note-content {
    flex-grow: 1;
    font-size: 0.95em;
    line-height: 1.7;
    margin-bottom: 15px;
}
/* 文字环绕附件样式 */
.attachment-wrapper {
    float: left;
    margin: 0 15px 10px 0;
    max-width: 45%;
    border-radius: 8px;
    overflow: hidden;
}
.attachment-wrapper img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}
/* 清除浮动 */
.clearfix::after {
    content: "";
    display: table;
    clear: both;
}
.note-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.85em;
    color: #777;
    padding-top: 15px;
    border-top: 1px solid #f0f0f0;
}
.source-link {
    flex-grow: 1;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}
.edit-btn {
    color: #4da6ff;
    text-decoration: none;
    margin-left: 10px;
    opacity: 0.7;
}
.note-date {
    background: #f0f7ff;
    color: #1e88e5;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.8em;
}
.stats-bar {
    display: flex;
    justify-content: space-around;
    background: linear-gradient(120deg, #e0f2ff, #f9f7ff);
    padding: 15px;
    border-radius: 12px;
    margin-bottom: 25px;
}
.stat-item {
    text-align: center;
}
.stat-value {
    font-size: 1.8em;
    font-weight: bold;
    color: #5e35b1;
}
.stat-label {
    font-size: 0.9em;
    color: #666;
    margin-top: 5px;
}
.empty-state {
    text-align: center;
    padding: 50px 20px;
    color: #777;
    font-size: 1.1em;
    border: 2px dashed #eaeaea;
    border-radius: 15px;
    background: #fafafa;
    margin-top: 30px;
}
.empty-icon {
    font-size: 4em;
    opacity: 0.3;
    margin-bottom: 20px;
}
`;
document.head.appendChild(style);

// 空状态处理
if (sortedNotes.length === 0) {
    const emptyDiv = dv.el('div', '', { cls: 'empty-state' });
    emptyDiv.appendChild(dv.el('div', '📚', { cls: 'empty-icon' }));
    emptyDiv.appendChild(dv.el('h3', '未找到书籍笔记'));
    emptyDiv.appendChild(dv.el('p', '请使用以下格式在笔记中添加书籍内容：'));
    emptyDiv.appendChild(dv.el('p', '书籍:: ![[附件]] 文字内容', { 
        style: 'font-family: monospace; background: #f5f5f5; padding: 5px; border-radius: 4px;' 
    }));
    container.appendChild(emptyDiv);
    dv.container.appendChild(container);
    return;
}

// 创建统计信息栏
const statsBar = dv.el('div', '', { cls: 'stats-bar' });

const statsData = [
    { value: sortedNotes.length, label: '笔记总数' },
    { value: new Set(sortedNotes.map(n => n.sourceNote)).size, label: '来源文件' },
    { value: sortedNotes.filter(n => n.content.includes('![')).length, label: '包含附件' }
];

statsData.forEach(stat => {
    const item = dv.el('div', '', { cls: 'stat-item' });
    item.appendChild(dv.el('div', stat.value, { cls: 'stat-value' }));
    item.appendChild(dv.el('div', stat.label, { cls: 'stat-label' }));
    statsBar.appendChild(item);
});

container.appendChild(statsBar);

// 创建笔记网格
const grid = dv.el('div', '', { cls: 'note-grid' });

// 添加笔记卡片
for (const note of sortedNotes) {
    const card = dv.el('div', '', { cls: 'note-card' });
    
    // 笔记内容容器
    const contentDiv = dv.el('div', '', { 
        cls: 'note-content clearfix',
    });
    
    // 处理附件和文字内容
    let contentHtml = note.content;
    
    // 检测附件格式
    const attachmentRegex = /(!\[\[.*?\]\])/g;
    const attachments = [];
    
    // 分离附件和文字
    let lastIndex = 0;
    let match;
    let textParts = [];
    
    while ((match = attachmentRegex.exec(note.content)) !== null) {
        if (match.index > lastIndex) {
            textParts.push({
                type: 'text',
                content: note.content.substring(lastIndex, match.index)
            });
        }
        
        attachments.push({
            type: 'attachment',
            content: match[1]
        });
        
        lastIndex = match.index + match[0].length;
    }
    
    if (lastIndex < note.content.length) {
        textParts.push({
            type: 'text',
            content: note.content.substring(lastIndex)
        });
    }
    
    // 如果有附件，创建浮动容器
    if (attachments.length > 0) {
        const attachmentWrapper = dv.el('div', '', { cls: 'attachment-wrapper' });
        attachmentWrapper.appendChild(dv.span(attachments[0].content, { markdown: true }));
        contentDiv.appendChild(attachmentWrapper);
    }
    
    // 添加文字内容
    textParts.forEach(part => {
        if (part.content.trim() !== '') {
            const textSpan = dv.span(part.content, { markdown: true });
            contentDiv.appendChild(textSpan);
        }
    });
    
    card.appendChild(contentDiv);
    
    // 页脚区域
    const footer = dv.el('div', '', { cls: 'note-footer' });
    
    // 来源文件
    const sourceLink = dv.el('span', note.sourceNote.replace('.md', ''), {
        cls: 'source-link',
        title: note.sourceNote
    });
    footer.appendChild(sourceLink);
    
    // 编辑按钮
    const editBtn = dv.el('a', '编辑', {
        cls: 'edit-btn',
        attr: {
            href: `obsidian://advanced-uri?filepath=${encodeURIComponent(note.sourceFile)}&line=${note.lineNumber}`,
            title: '在源文件中编辑'
        }
    });
    footer.appendChild(editBtn);
    
    // 日期标签
    footer.appendChild(dv.el('span', note.date, { cls: 'note-date' }));
    
    card.appendChild(footer);
    grid.appendChild(card);
}

container.appendChild(grid);

// 标题
dv.header(2, `机会 (${sortedNotes.length}条)`);
dv.container.appendChild(container);
```