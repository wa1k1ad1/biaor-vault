---
项目查询:
  - 项目
时间跨度:
最近三天: true
最近三周: true
最近三月: true
最近三季: true
最近三年: true
---
##### AI 提示词
软件obsidian，dataview和charts插件，基于笔记属性「项目查询」查询
制作新的最近三天「堆叠条形图」代码功能，只有一个「条形图视图」没有多余的详情内容列表
- 条形图坐标：✅[[当前笔记：项目瓶颈问题（项目用，显示反向链接）]]/❌[[当前笔记：项目瓶颈问题（项目用，显示反向链接）]]（支持点击跳转页面）
- 条形图进度条1：堆叠（日期期限 and 专注时长）
- 条形图进度条2：瓶颈问题：堆叠（完成数量 and 未完成数量）
- 条形图进度条3：解决方案数量
- 条形图排序：进度条1，日期期限倒序


日期期限计算：创建日期~now time（现在日期）/完成日期/截止日期）
专注时长例如25分钟、1小时、1天：【⏳00:25:00 】、【⏳01:00:00 】、【⏳24:00:00 】
日期期限1天例如：➕ 2025-12-01 ✅ 2025-12-01

##### 方案
###### 方法一：Dataview 查询（基础版本）
```dataview
TASK
FROM ""
WHERE !completed
AND created >= date(today) - dur(3 days)
SORT file.ctime DESC
```
###### 方法二：处理缩进任务的查询
```dataview
TASK
FROM ""
WHERE contains(text, "[[项目") 
OR contains(text, "🏁")
SORT file.ctime DESC
```
###### 方法三：显示完整任务树结构
```dataview
TASK
FROM ""
WHERE file.ctime >= date(today) - dur(3 days)
OR contains(text, "[[项目B]]")
OR contains(text, "🏁")
GROUP BY file.link
```
###### 方法四：处理特定标记的查询
```dataview
TASK
FROM ""
WHERE contains(text, "[[项目") 
OR contains(text, "🏁")
OR contains(text, "➕")
SORT file.ctime DESC
LIMIT 20
```
###### 方法五：基于属性「项目查询」检索任务
```dataview
TASK
FROM ""
WHERE any(this.项目查询, (p) => contains(text, p))
SORT file.ctime DESC
```